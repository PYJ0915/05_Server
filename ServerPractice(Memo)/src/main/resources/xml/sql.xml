<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	
	<!-- 메모 목록 조회 -->
	<entry key="getMemoFull">
		SELECT MEMO_NO, MEMO_TITLE, MEMO_CONTENT, TO_CHAR(REG_DATE, 'YYYY-MM-DD HH24:MI:SS') REG_DATE 
		FROM PRACTICE_MEMO ORDER BY MEMO_NO
	</entry>
	
	<!-- 메모 상세 조회 -->
	<entry key="memoDetail">
		SELECT MEMO_TITLE, MEMO_CONTENT, TO_CHAR(REG_DATE, 'YYYY-MM-DD HH24:MI:SS') REG_DATE 
		FROM PRACTICE_MEMO WHERE MEMO_NO = ?
	</entry>
	
	<!-- 메모 추가 -->
	<entry key="addMemo">
		INSERT INTO PRACTICE_MEMO
		VALUES(SEQ_MEMO_NO.NEXTVAL, ?, ?, DEFAULT)
	</entry>
	
	<!-- 메모 삭제 -->
	<entry key="deleteMemo">
		DELETE FROM PRACTICE_MEMO
		WHERE MEMO_NO = ?
	</entry>
	
	<!-- 메모 수정 -->
	<entry key="updateMemo">
		UPDATE PRACTICE_MEMO 
		SET MEMO_TITLE = ?, MEMO_CONTENT = ?
		WHERE MEMO_NO = ?
	</entry>

</properties>
